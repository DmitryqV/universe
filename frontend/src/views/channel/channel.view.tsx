import React, { FunctionComponent } from "react";
import { Layout } from "@layouts/export";

export const Channel: FunctionComponent = () => {
  return (
    <Layout.Responsive>
      <section>channel view</section>
    </Layout.Responsive>
  );
};
